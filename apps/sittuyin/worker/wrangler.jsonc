{
  "$schema": "node_modules/wrangler/config-schema.json",
  // The Sittuyin product's own Worker: static assets, /api/*, /ws/*, its Durable Objects and its own D1.
  // It shares no binding, database or domain with Makruk (docs/PLATFORM.md).
  "name": "sittuyin",
  "main": "src/index.ts",
  "compatibility_date": "2026-09-01",
  "assets": {
    "directory": "../web/dist",
    "binding": "ASSETS",
    "not_found_handling": "single-page-application",
    "run_worker_first": ["/api/*", "/ws/*"]
  },
  "durable_objects": {
    "bindings": [
      { "name": "GAME_ROOM", "class_name": "GameRoom" },
      { "name": "MATCHMAKER", "class_name": "Matchmaker" }
    ]
  },
  "migrations": [{ "tag": "v1", "new_sqlite_classes": ["GameRoom", "Matchmaker"] }],
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "sittuyin",
      // Production database, created 2026-09-14 (sit-009). Local dev and tests use a local database.
      "database_id": "2645cb45-b6a3-4df2-b953-439d2648cd47",
      "migrations_dir": "migrations"
    }
  ],
  "vars": {
    "RECONNECT_GRACE_MS": "60000"
  },
  // The subdomain the owner chose (sit-009). Moving domains: change this and the default in
  // apps/sittuyin/web/site.config.ts, nothing else.
  "routes": [{ "pattern": "my-chess.beanroti.com", "custom_domain": true }],
  "observability": { "enabled": true }
}
