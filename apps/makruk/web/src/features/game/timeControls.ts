/** Time-control presets and conversion are shared by every game site (@chaturanga/game-shell). */
export {
  CUSTOM_LIMITS,
  PRESETS,
  QUICK_MATCH_PRESET_IDS,
  TIME_CATEGORIES,
  type TimeCategory,
  type TimeControl,
  type TimeControlChoice,
  type TimeControlPreset,
  toTimeControl,
} from '@chaturanga/game-shell';
