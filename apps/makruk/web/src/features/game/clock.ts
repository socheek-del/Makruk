/** Clock math is shared with the server via @chaturanga/makruk/clock. */
export {
  type ClockState,
  createClock,
  flaggedSide,
  flagTime,
  pressClock,
  runFor,
  stopClock,
  timesAt,
} from '@chaturanga/makruk/clock';
