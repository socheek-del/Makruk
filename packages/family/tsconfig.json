{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // The app configs it reads import types from @chaturanga/game-shell, whose sources use the DOM.
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "types": ["node"]
  },
  "include": ["src"]
}
