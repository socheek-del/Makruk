/**
 * The Chaturanga family: which games exist and what each is called in every language a family site
 * speaks. That is all a site may know about its siblings — their addresses come from each game's own
 * `site.config.ts` (see `sites.ts`), and nothing else crosses from one product to another.
 */
export { type FamilyGame, type FamilyLanguage, type GameId, GAMES } from './games';
export type { FamilyLink } from './sites';
